# Директория lib/ (Вспомогательные функции)

 * `string.c` – базовые операции со строками (strcmp, memcpy).
 * `ctype.c` – функции работы с символами (isdigit, isalpha).

📌 Это аналог стандартной библиотеки libc, но для ядра.
